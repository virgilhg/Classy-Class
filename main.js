// Note: Code incomplete. Challenge was time sensitive but with more time, I would have added get behind my info method to make it a getter property
class Person {
  constructor(name, age) {
    this.name= name;
    this.age = age;
  }
    Info(){
      return `${this.name} age is ${this.age}`
    } 
}
